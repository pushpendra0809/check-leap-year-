#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a, b,c,d ;
    cin>>a;
    if (a%100==0)
    {
        if(a%400==0)
        cout<<"leap yrear";
    }
    else
    if (a%4==0)
    cout <<"leap year";
    else
    cout <<"not leap year";
}
